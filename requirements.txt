annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.2.25
click==8.3.1
colorama==0.4.6
distro==1.9.0
fastapi==0.135.2
groq==1.1.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
pydantic==2.12.5
pydantic_core==2.41.5
python-dotenv==1.2.2
python-multipart==0.0.22
sniffio==1.3.1
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.42.0
websockets==16.0

# ── Phase 7: Pipecat pipeline framework ──────────────────────────────────────
pipecat-ai>=0.0.60   # core pipeline framework (FrameProcessor, Pipeline, PipelineTask)
loguru>=0.7.0        # structured logging used by pipecat internally

# ── Phase 8: LangGraph memory + agent flow ────────────────────────────────────
langgraph>=0.2.0     # state graph engine + MemorySaver checkpointer
langchain-core>=0.3.0  # HumanMessage, AIMessage, BaseMessage types

# ── New features ──────────────────────────────────────────────────────────────
ddgs>=7.0.0               # Feature 9: web search tool (renamed from duckduckgo-search)
langdetect>=1.0.9          # Feature 7: language detection fallback

# ── Silero VAD: neural voice activity detection ───────────────────────────────
torch>=2.0.0         # PyTorch runtime (CPU-only is fine, no GPU needed)
torchaudio>=2.0.0    # required by silero-vad for audio utilities
silero-vad>=5.1.2    # Silero VAD model loader (wraps torch.hub)

# ── Phase 1: Qdrant Cloud vector database ────────────────────────────────────
qdrant-client>=1.9.0          # async Qdrant client (AsyncQdrantClient)

# ── Phase 2: Embeddings (local, CPU, free) ───────────────────────────────────
sentence-transformers>=3.0.0  # multilingual-e5-small → 384-dim multilingual embeddings
pypdf>=4.0.0                  # PDF text extraction (legacy loader, kept for compat)
pdfplumber>=0.11.0            # layout-aware PDF parsing for smart ingestor

# ── Phase 4: LangSmith observability ─────────────────────────────────────────
langsmith>=0.1.0              # tracing + eval client

# ── Phase 5: Dream Cycle analysis ────────────────────────────────────────────
numpy>=1.26.0                 # array ops for clustering
scikit-learn>=1.4.0           # KMeans clustering for PromptImprovementCycle
